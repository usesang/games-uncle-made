# 큰아빠가 만든 게임들 — UI·게임 화면 기준

이 문서는 모음집과 앞으로 추가할 모든 게임의 공통 설계 기준입니다. 새 게임을 시작할 때 이 기준을 먼저 적용하고, 예외가 필요하면 게임 README에 이유를 기록합니다.

## 1. 화면과 안전 영역

- 모음집의 제목·링크와 게임의 정보·조작 버튼은 노치, 상태 표시줄, 홈 제스처 영역에 겹치지 않게 배치합니다. `viewport-fit=cover`로 배경은 끝까지 채우되 중요한 내용은 `env(safe-area-inset-*)`와 최소 여백을 함께 적용합니다.
- iPhone 홈 화면 웹앱에서도 시계·배터리 표시가 남을 수 있습니다. 웹에서 강제로 숨기는 것을 전제로 설계하지 않습니다.
- 세로 전용 화면은 세로에, 가로 전용 게임은 기기를 실제로 돌렸을 때 가로에 표시합니다. 방향 전환으로 플레이가 불가능해지면 자동 일시정지합니다.
- 게임 화면은 사용 가능한 뷰포트를 채웁니다. 고정 16:9를 늘리거나 잘라서 맞추지 않습니다. 캔버스 게임은 세로 논리 크기와 캐릭터 비율을 유지하고, 기기의 가로세로 비율에 따라 **보이는 월드의 가로 폭**을 조정합니다.
- 월드 폭이 달라지면 카메라 경계, 배경, 반복 타일, 오브젝트 컬링, UI 위치를 함께 점검합니다. 시스템이 점유하는 안전 영역은 조작 가능 영역으로 계산하지 않습니다.

## 2. 조작과 메뉴

- 모바일 플레이 중 상단에는 **일시정지**를 우선 배치합니다. 모음집 이동·음량·조작 설정처럼 덜 자주 쓰는 기능은 일시정지 메뉴에 둡니다.
- 터치 대상은 최소 44×44 CSS px, 가능하면 48×48 CSS px 이상으로 만듭니다. 보이는 아이콘보다 실제 누를 수 있는 영역이 작아서는 안 됩니다. 화면 가장자리와 다른 버튼 사이에도 여백을 둡니다.
- 이동과 점프 등 동시 입력을 지원합니다. 멀티터치 중 화면 확대·글자 선택·스크롤이 게임 조작을 방해하지 않게 하되, 설정 대화상자의 슬라이더 등 필요한 조작은 유지합니다.
- 조작 버튼의 크기와 불투명도를 조절할 수 있게 하고, 선택한 값은 기기에 저장합니다. 버튼은 배경을 가리지 않도록 반투명하게 표시하되 글자 대비와 눌림 상태가 분명해야 합니다.
- 키보드와 터치 입력은 동일한 게임 동작을 호출합니다. 버튼에는 접근 가능한 이름을 붙이고, 일시정지·재개·스테이지 완료에는 분명한 상태를 표시합니다.

## 3. 게임 세계와 시각 정렬

- 충돌 좌표와 그림의 기준점을 따로 기록합니다. 캐릭터의 발, 바닥 표면, 발판 윗면은 같은 월드 좌표에 맞춥니다.
- 스테이지별 그림을 바꿀 때 투명 여백이나 그림 속 장식 때문에 발이 떠 보이지 않는지 확인합니다. 물리 법칙을 임의로 바꾸기 전에 임시 충돌선으로 원인을 확인합니다.
- 달리기·점프 애니메이션의 프레임마다 발 위치가 다를 수 있으므로, 착지 프레임은 기준 발 위치로 보정합니다. 공중 프레임은 의도된 높이 변화만 허용합니다.
- 화면 폭이 늘어나도 바닥·배경이 끊기지 않아야 합니다. 장애물, 적, 둥지 등 월드 오브젝트는 화면 크기와 무관하게 월드 좌표에 고정합니다.

## 4. 출시 전 확인 목록

- iPhone 홈 화면 앱, Safari 탭, Android Chrome, 데스크톱에서 모음집 제목과 게임 HUD가 시스템 UI에 가리지 않는지 확인합니다.
- 가로로 긴 휴대폰·일반 휴대폰·태블릿·데스크톱에서 게임이 찌그러짐이나 불필요한 좌우 여백 없이 보이는지 확인합니다.
- 일시정지, 재개, 음량, 설정, 모음집 이동이 한 손 터치로 작동하는지 확인합니다. 특히 가장자리 버튼을 반복해서 눌러 봅니다.
- 각 스테이지의 시작 지점과 모든 발판에서 정지·달리기·점프·착지 시 발과 지면이 맞는지 확인합니다.
- 모든 그림을 불러오기 전에는 플레이를 시작하지 못하게 하고, 스테이지 전환·재시도 후에도 UI 상태가 올바른지 확인합니다.

## 5. 모음집 공통 원칙

- 새 게임은 `games/<게임-이름>/`에 독립적으로 두고 모음집 카드로 연결합니다.
- 제목, 조작 설명, 난이도, 재시도 방법, 저작권·문의 링크의 표현을 게임마다 일관되게 유지합니다.
- 공개 저장소에는 아이들의 원본 사진과 개인 정보를 넣지 않습니다. 배포에는 공개용 캐릭터 아트만 포함합니다.
